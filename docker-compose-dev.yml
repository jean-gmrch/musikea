x-service: &service
  env_file: ./.env
  restart: always 

services:
  backend:
    <<: *service
    container_name: backend-dev
    build: ./backend

  frontend:
    <<: *service
    container_name: frontend-dev
    build: ./frontend
    depends_on:
      - backend
  
  nginx:
    <<: *service
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx:/etc/nginx/conf.d
    depends_on:
      - frontend